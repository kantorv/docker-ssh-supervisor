Base ssh/supervisor cont.

IMPORTANT!
Remeber to replace key pair by your own:

$ rm sshkey.pem sshkey.pub
$ ssh-keygen -t rsa -b 2048 -f sshkey.pem
$ chmod 400  sshkey.pem
$ mv sshkey.pem.pub sshkey.pub
